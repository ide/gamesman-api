package edu.berkeley.gamesman.api;

/**
 * The core interface that defines the back-end Gamesman API.
 * <p>
 * Ideally, implementations of the API will be thread-safe since a single API
 * object may need to service multiple concurrent requests. In the event that an
 * API implementation is <em>not</em> thread-safe, users of the class must
 * synchronize access to the API.
 *
 * @author James Ide
 */
public interface GamesmanApi {
    
    // Should use typed return values to enforce validity
    
    // get move value
    
    // get next moves
    
    // get next move values
    
    // 
    
    // solve -- user must be authenticated!
    
    // 
}
